# Puppet Hiera Data
version: 5

defaults:
  datadir: .
  data_hash: yaml_data

hierarchy:
  - name: hierarchy
    paths:
      - "/vagrant/puppet/%{environment}.yaml"
      - /vagrant/puppet/common.yaml
      - /vagrant/vagrant-nsidc.yaml
